Simple Pacman arcade game controlled by the arrow-keys
Use the command "pip install -r requirements.txt" in the terminal to download the necessary package versions.

If you're having troubles running it, make sure it's running in a venv.